export const selectCampers = (state) => state.campers.items;
export const selectCamperFavorite = (state) => state.campers.favorites;
export const selectIsLoading = (state) => state.campers.isLoading;
